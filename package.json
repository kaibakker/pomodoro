{
  "name": "pomodoro",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://kaibakker.github.io/pomodoro",
  "devDependencies": {
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "moment": "^2.17.1",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-sortable-hoc": "^0.4.12"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build"
  }
}
